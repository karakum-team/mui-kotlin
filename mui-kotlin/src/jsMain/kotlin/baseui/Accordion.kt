// Automatically generated - do not modify!

@file:JsModule("@base-ui/react/accordion")

package baseui

import react.FC

/**
 * `export * as Accordion` from `@base-ui/react/accordion` — the module's only value export.
 *
 * The package's `exports` map has no wildcard entry, so a part's own subpath is not importable: this
 * object is the only way to reach a part at runtime.
 */
external object Accordion {
    val Header: FC<AccordionHeaderProps>
    val Item: FC<AccordionItemProps>
    val Panel: FC<AccordionPanelProps>
    val Root: FC<AccordionRootProps>
    val Trigger: FC<AccordionTriggerProps>
}
