// Automatically generated - do not modify!

@file:JsModule("@base-ui/react/field")

package baseui

import react.FC

/**
 * `export * as Field` from `@base-ui/react/field` — the module's only value export.
 *
 * The package's `exports` map has no wildcard entry, so a part's own subpath is not importable: this
 * object is the only way to reach a part at runtime.
 */
external object Field {
    val Control: FC<FieldControlProps>
    val Description: FC<FieldDescriptionProps>
    val Error: FC<FieldErrorProps>
    val Item: FC<FieldItemProps>
    val Label: FC<FieldLabelProps>
    val Root: FC<FieldRootProps>
    val Validity: FC<FieldValidityProps>
}
